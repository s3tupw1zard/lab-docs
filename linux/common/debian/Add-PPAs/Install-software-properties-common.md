First install `software-properties-common` and `apt-transport-https`:

```zsh
sudo apt install software-properties-common apt-transport-https
```

After that, the `add-apt-repository` command should work.